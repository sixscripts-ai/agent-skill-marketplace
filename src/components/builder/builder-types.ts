export type BuilderVisibility = "public" | "private" | "unlisted";

export type BuilderViewMode = "markdown" | "canvas";

export type BuilderSavedUrls = {
  detail: string;
  marketplace: string;
  mySkills: string;
  run: string;
  edit: string;
};
