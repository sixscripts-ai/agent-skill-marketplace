# Skill References

Load conditions for this converter skill:

- Read `references/claude-skill-format.md` if|when the source skill comes from Claude Code, `.claude/skills`, or a Claude plugin cache with `upstream/` overlays.
- Read `references/cursor-skill-format.md` if|when writing Cursor frontmatter or choosing `.cursor/skills` vs `~/.cursor/skills`.
- Read `references/conversion-rules.md` if|when deciding which files to copy, rewrite, skip, or refuse.
